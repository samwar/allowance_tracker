# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page


GET     /                           controllers.AllowanceController.allowance()
POST    /allowance                  controllers.AllowanceController.addAllowance()
GET     /purchase                   controllers.PurchaseController.purchase()
POST    /addPurchase                controllers.PurchaseController.addPurchase()


# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
